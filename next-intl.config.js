// This file is used by next-intl to configure the locales and default locale
// It's imported by the next-intl plugin in next.config.js

module.exports = {
  // A list of all locales that are supported
  locales: ['en', 'es', 'zh', 'fr', 'pt', 'ar', 'hi'],
  
  // If this locale is matched, pathnames work without a prefix (e.g. `/about`)
  defaultLocale: 'en',
};